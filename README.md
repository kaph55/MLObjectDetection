# MLObjectDetection
Object Detection with C# and ML.NET (using Roboflow dataset) Detect and classify objects in images and videos with a custom-trained model. Dataset prepared and exported from Roboflow, then used in C# applications via ML.NET (.mlconfig) for both Console and WPF apps. Provides real-time object detection with bounding boxes and class labels.
